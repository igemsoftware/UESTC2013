1 Unzip the download file.
2 The command line switch to the current directory
3 Copy your fasta files to the current directory 
  //The Standalone only supports fasta format. see the "Example.fasta".
4 Type
  "python transpeeder.py inputfile > outputfile"
  on command line for running.

  
